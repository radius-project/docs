---
type: docs
title: "rad rollback CLI reference"
linkTitle: "rad rollback"
slug: rad_rollback
url: /reference/cli/rad_rollback/
description: "Details on the rad rollback Radius CLI command"
---
## rad rollback

Rolls back Radius for a given platform

### Synopsis

Rolls back Radius for a given platform

### Options

```
  -h, --help   help for rollback
```

### Options inherited from parent commands

```
      --config string   config file (default "$HOME/.rad/config.yaml")
  -o, --output string   output format (supported formats are json, table) (default "table")
```

### SEE ALSO

* [rad]({{< ref rad.md >}})	 - Radius CLI
* [rad rollback kubernetes]({{< ref rad_rollback_kubernetes.md >}})	 - Rolls back Radius on a Kubernetes cluster

